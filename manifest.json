{
  "mcpb_version": "0.1",
  "name": "panos-mcp",
  "version": "1.0.0",
  "description": "MCP server for Palo Alto Networks PanOS firewalls and Panorama",
  "author": { "name": "Apius Tech" },
  "server": {
    "type": "node",
    "entry_point": "server/index.js",
    "mcp_config": {
      "command": "node",
      "args": ["${__dirname}/server/index.js"],
      "env": {
        "PANOS_HOST": "${user_config.host}",
        "PANOS_API_KEY": "${user_config.api_key}"
      }
    }
  },
  "user_config": {
    "host": {
      "type": "string",
      "title": "Firewall Host",
      "description": "PanOS firewall or Panorama hostname or IP address",
      "required": true
    },
    "api_key": {
      "type": "string",
      "title": "API Key",
      "description": "PanOS API key (generate with: npx panos-mcp keygen)",
      "sensitive": true,
      "required": true
    }
  }
}
